Read {{BUSDIR}}/task.md and {{BUSDIR}}/brief.txt for full context and the exact output contracts. You are the grok implementation pane for {{TASK}}. You are already inside a fresh git worktree created for this task at launch (the --worktree flag) — work there and do NOT create, switch, or nest another worktree. {{FEEDBACK_STEP}}Build the brief by running /implement --effort 3 (its --effort is the reviewer COUNT, integer 1-5 — NOT a model effort level); do NOT enter /plan mode — it blocks edits and would hang this unattended pane (if you want a plan first, write it to {{BUSDIR}}/plan.md with your file-write tool, then implement). Commit your work in the worktree with a clear message. Then run the local gate in the worktree — {{GATE_STEP}} — and write {{ITERDIR}}/gate.md exactly per the contract, with the last content line GATE: PASS or GATE: FAIL then a final line END-OF-FILE and nothing after it. Finally write {{ITERDIR}}/result.md exactly per the contract: first line WORKTREE: followed by the absolute worktree toplevel from git rev-parse --show-toplevel, second line BRANCH: followed by the actual git branch from git rev-parse --abbrev-ref HEAD, then a short summary, then a final line END-OF-FILE and nothing after it. Use your file-write tool. Then stop.
