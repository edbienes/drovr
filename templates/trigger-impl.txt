Read {{BUSDIR}}/task.md and {{BUSDIR}}/brief.txt for full context and the exact output contracts. You are the implementation pane for {{TASK}}. {{WORKTREE_STEP}}{{FEEDBACK_STEP}}When the brief is non-trivial and {{BUSDIR}}/plan.md does not exist yet, design before coding: write an implementation plan (the superpowers writing-plans skill works well) to {{BUSDIR}}/plan.md, then proceed — never enter interactive plan mode, it pauses the session on an approval dialog no one at this pane will answer. Implement the brief, then commit your work in the worktree with a clear message. Then run the local gate in the worktree — {{GATE_STEP}} — and write {{ITERDIR}}/gate.md exactly per the contract, with the last content line GATE: PASS or GATE: FAIL then a final line END-OF-FILE and nothing after it. Finally write {{ITERDIR}}/result.md exactly per the contract: first line WORKTREE: followed by the absolute worktree toplevel from git rev-parse --show-toplevel, second line BRANCH: followed by the actual git branch from git rev-parse --abbrev-ref HEAD, then a short summary, then a final line END-OF-FILE and nothing after it. Use your Write tool. Then stop.
